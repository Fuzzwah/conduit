{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.rs) rustfmt --edition 2021 \"$f\" ;; esac; } 2>/dev/null || true"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -E '/web/.*\\.(ts|tsx)$' | { read -r f; [ -n \"$f\" ] && (cd /home/fuz/code/conduit/web && npx eslint --fix \"$f\"); } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
