## Context

`README.md` currently has a title, one sentence, and a usage block. It needs a project description and a changelog section.

## Goals / Non-Goals

**Goals:**
- Add a one-line description of what `greet` does below the `# greet` heading.
- Add a `## Recent Updates` section at the bottom with an initial entry.

**Non-Goals:**
- Changing `src/main.rs` or `Cargo.toml`.
- Adding badges, CI status, or contribution guides.

## Decisions

Edit `README.md` directly — no tooling or scripts required.
