# Dirac CLI

Dirac CLI is a multi-provider coding agent with plan mode support.

## Installation

```bash
npm install -g dirac-cli
```

## Notes

- Conduit starts Dirac in JSON mode for machine-readable output.
- Plan mode is supported.
- Imported Dirac history is not supported yet.
